<Window x:Class="ZoneHydrantEditor.UserInput.BindingEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование привязки"
        Height="350"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Комментарий:" FontWeight="SemiBold" Margin="0,0,10,8" VerticalAlignment="Center" />
        <TextBox x:Name="CommentTextBox" Grid.Column="1" Margin="0,0,0,8" ToolTip="Место расположения привязки" />

        <TextBlock Text="Широта:" FontWeight="SemiBold" Grid.Row="1" Margin="0,0,10,8" VerticalAlignment="Center" />
        <TextBox x:Name="LatitudeText" Grid.Row="1" Grid.Column="1" IsReadOnly="True" Background="#F0F0F0" Margin="0,0,0,8" />

        <TextBlock Text="Долгота:" FontWeight="SemiBold" Grid.Row="2" Margin="0,0,10,8" VerticalAlignment="Center" />
        <TextBox x:Name="LongitudeText" Grid.Row="2" Grid.Column="1" IsReadOnly="True" Background="#F0F0F0" Margin="0,0,0,8" />

        <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,5,0,10" />

        <TextBlock Text="Влево (м):" FontWeight="SemiBold" Grid.Row="4" Margin="0,0,10,8" VerticalAlignment="Center" />
        <TextBox x:Name="LeftText" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8" ToolTip="Расстояние от привязки до ПГ налево (в метрах)" />

        <TextBlock Text="Вправо (м):" FontWeight="SemiBold" Grid.Row="5" Margin="0,0,10,8" VerticalAlignment="Center" />
        <TextBox x:Name="RightText" Grid.Row="5" Grid.Column="1" Margin="0,0,0,8" ToolTip="Расстояние от привязки до ПГ направо (в метрах)" />

        <TextBlock Text="Прямо (м):" FontWeight="SemiBold" Grid.Row="6" Margin="0,0,10,8" VerticalAlignment="Center" />
        <TextBox x:Name="StraightText" Grid.Row="6" Grid.Column="1" Margin="0,0,0,8" ToolTip="Расстояние от привязки до ПГ по прямой (в метрах)" />

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Сохранить" Width="120" Height="35" Margin="0,0,10,0"
                    Background="#1E1E1E" Foreground="White" FontWeight="SemiBold"
                    Click="SaveButton_Click" IsDefault="True" />
            <Button Content="Отмена" Width="100" Height="35"
                    Background="#E8E8E8" FontWeight="SemiBold"
                    Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
